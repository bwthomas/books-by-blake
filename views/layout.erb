<!DOCTYPE html>
<html>
  <head>
    <title>Books, Y'all!</title>
    <style type="text/css">
        .results { display: table; width: 50%; }
        .result { display: table-row; }
        .info {
            display: table-cell;
            vertical-align: top;
            border: none;
            padding-left: 1ex;
            padding-right: 1ex;
        }
        .thumb {
          width: 9em;
        }
    </style>
  </head>
  <body>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
